// --------- Example : Square root of a number ---- //

// we can use javascript built in method ---- Math.sqrt(Number)

const number = 20;

const result = Math.sqrt(number);
console.log(
    `The number is ${number} and the square root of number is ${result}`
);
